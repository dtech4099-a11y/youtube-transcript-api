This directory is reserved for optional request middleware modules.

Route-level wrappers in `lib/api-handler.ts` currently handle authentication, logging, and rate limiting so the API stays explicit and easy to test.
